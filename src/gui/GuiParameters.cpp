#include "gui/GuiParameters.hpp"

namespace GuiParameters {

const sf::Color backgroundColor(20, 20, 25, 150);
const sf::Color foregroundColor(45, 50, 55, 230);
const float outlineThickness = 2;
const sf::Color outlineColor(100, 105, 115);
unsigned slotSizeU = 80u;
float slotSizeF = static_cast<float>(slotSizeU);
const sf::Color highlightColor = outlineColor;

}